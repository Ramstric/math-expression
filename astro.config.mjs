// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://ramstric.github.io',
    base: 'cnn-math-expressions-website',
  })